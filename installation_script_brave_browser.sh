
echo "This will install the Brave Browser of Version: $version in this machine."

echo "This will install the brave browser in this machine."

# sudo dpkg -i *.deb
sudo apt install ./brave-*.deb
# It should install only brave & brave keyring applicaion.

echo "To Open Brave Browser Application, brave-browser."

echo "To check brave browser version, run this: brave-browser --version"

